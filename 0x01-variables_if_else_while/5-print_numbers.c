#include <stdio.h>

/**
 * main - prints all single digits of base 10 from 0
 * followed by new line
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int x = 0;

	for (x=0; x<=9; x++;)
	{
		printf("%d\n, x");
	}
 	return (0);
}
