#include <stdio.h>

/**
 * main -prints lowercase letters
 * of the alphabet
 * 
 * Return : Always 0 (success)
 */

int main(void)
{
	char XX;

	for (XX = 'a'; XX <= 'z'; XX++)
	{
		putchar(XX);
	}
	return (0);
}
