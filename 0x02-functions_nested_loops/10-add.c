#include "main.h"

/**
 * int add - adds two integers and returns the result
 * @n: character to be computed
 *
 * Return: Always 0 (success)
 */

int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
