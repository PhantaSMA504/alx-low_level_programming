#include "main.h"

/**
 * print_numbers - prints numbers from 0-9
 * excluding 2 and 4
 * followed by new line
 * Return: void
 */

void print_numbers(void)

{
	int a;

	for (a = 48; a <= 57; a++)
	{
		if (a = 50 && a = 52)
		{
			continue;
		}
		else
		{
			_putchar(a);
		}
	}
	_putchar('\n');
}
